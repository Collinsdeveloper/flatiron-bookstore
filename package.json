{
  "name": "flatiron-bookstore",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout 5000 --reporter mocha-junit-reporter --reporter-options mochaFile=test-results.xml"
  },
  "devDependencies": {
    "jsdom": "^22.1.0",
    "mocha": "^10.2.0",
    "mocha-junit-reporter": "^2.2.1"
  }
}
